ARG BASE_IMAGE=clawbernetes/hermes:latest-base
FROM ${BASE_IMAGE}

# TODO: Install Hermes-specific plugins when available.
# Placeholder — the Hermes agent runtime does not yet have a plugin ecosystem.
